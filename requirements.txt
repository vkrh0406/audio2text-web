fastapi==0.117.1
uvicorn[standard]==0.37.0
python-multipart==0.0.20
jinja2==3.1.6
pydantic==2.11.9
# Speech-to-text engine (local, offline). Requires ffmpeg installed on the host.
faster-whisper==1.2.0
# Optional: for WAV utilities (not strictly required)
soundfile==0.13.1
redis==6.4.0
